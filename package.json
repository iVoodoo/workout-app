{
	"name": "workout-app",
	"version": "1.0.0",
	"engines": {
		"node": "16.17.0"
	},
	"description": "",
	"main": "server.js",
	"type": "module",
	"scripts": {
		"server": "nodemon back/server",
		"server-start": "node back/server",
		"start": "concurrently \"npm install\" \"npm run server-start\" \"npm run start --prefix front\"",
		"dev": "concurrently \"npm install\" \"npm run server\" \"npm run start --prefix front\"",
		"build": "cd front && npm run build",
		"install-client": "cd front && npm install",
		"heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix front && npm run build --prefix front"
	},
	"author": "voodoo",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"cors": "^2.8.5",
		"dotenv": "^16.0.1",
		"express": "^4.18.1",
		"express-async-handler": "^1.2.0",
		"http-proxy-middleware": "^2.0.6",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^6.5.3",
		"sass": "^1.54.9"
	},
	"devDependencies": {
		"colors": "^1.4.0",
		"concurrently": "^7.4.0",
		"morgan": "^1.10.0",
		"nodemon": "^2.0.19"
	}
}
